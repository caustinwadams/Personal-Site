<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
  <%= link_to "Austin Adams", root_path, class: "navbar-brand mb-0 h1" %>
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>
  <div class="collapse navbar-collapse" id="navbarSupportedContent">
    <ul class="navbar-nav mr-auto">
      <li class="<%= active_class(root_path) %>">
        <%= link_to "Home", root_path, class: "nav-link" %>
      </li>
      <li class="<%= active_class(resume_path) %>">
        <%= link_to "Resume", resume_path, class: "nav-link" %>
      </li>
      <li class="<%= active_class(blogs_path) %>">
        <%= link_to "Blog", blogs_path, class: "nav-link" %>
      </li>
      <% if logged_in? %>
        <li class="<%= active_class(new_blog_path) %>">
          <%= link_to "New Blog", new_blog_path, class: "nav-link" %>
        </li>
      <% end %>
    </ul>

    <% if logged_in? %>
      <div class="col-sm-2" style="padding: 0;">
        <%= link_to "Log Out", logout_path, method: :delete, 
                                            class: 'btn btn-primary',
                                            style: 'width: 100%;' %>
      </div>
    <% else %>
      <div class="col-sm-2" style="padding: 0;">
        <%= link_to "Log In", login_path, class: 'btn btn-primary', 
                                          style: 'width: 100%;' %>
      </div>
    <% end %>
  </div>
</nav>
